docker run -p 5000:5000 flask-project

When you run a container without specifying port mappings, the container's network is isolated, and external systems can't access it directly. That's why the URL might not be responding.